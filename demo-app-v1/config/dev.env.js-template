
// This file is meant to be duplicated and filled with your local settings
// When duplicating, remove '-template' from the extension

'use strict'
const merge = require('webpack-merge')
const prodEnv = require('./prod.env')

module.exports = merge(prodEnv, {
  NODE_ENV: '"development"',
  GOOGLE_API: 'ADD_YOUR_KEY_HERE'
})
